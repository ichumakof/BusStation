<!-- AdminApp/Views/ReportsWindow.xaml -->
<Window x:Class="AdminApp.Views.ReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Отчеты по продажам" Height="500" Width="700" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#2196F3" Padding="15" Grid.ColumnSpan="3" Margin="0,0,0,325" Grid.RowSpan="2">
            <TextBlock Text="ОТЧЕТЫ ПО ПРОДАЖАМ" 
              Foreground="White" 
              FontSize="18" 
              FontWeight="Bold"
              HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        
        <StackPanel Orientation="Horizontal" Margin="0,105,0,10">
            <TextBlock Text="Период: " VerticalAlignment="Center" Margin="0,0,10,0" FontSize="16"/>
            <DatePicker SelectedDate="{Binding FromDate}" Width="140" FontSize="16"/>
            <TextBlock Text="—" VerticalAlignment="Center" Margin="10,0"/>
            <DatePicker SelectedDate="{Binding ToDate}" Width="140" FontSize="16" />
        </StackPanel>

        <GroupBox Header="Выберите города назначения" Grid.Row="1" Margin="0,0,0,10" FontSize="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="300">
                <ItemsControl ItemsSource="{Binding Cities}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding CityName}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="5"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сформировать отчет" Command="{Binding GenerateReportCommand}" Margin="5" Padding="10,6" FontSize="16"/>
            <Button Content="Закрыть" Command="{Binding CancelCommand}" Margin="5" Padding="10,6" FontSize="16"/>
        </StackPanel>
    </Grid>
</Window>